{
  "name": "clawdbot-desktop",
  "version": "1.0.0",
  "description": "Clawdbot Desktop - Windows app with tray icon and gateway dashboard",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "pnpm run build:main && pnpm run build:preload && concurrently \"vite\" \"wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 node scripts/run-electron.js\"",
    "dev:vite": "vite",
    "dev:electron": "wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 node scripts/run-electron.js",
    "build": "pnpm run build:main && pnpm run build:preload && pnpm run build:renderer",
    "build:renderer": "vite build",
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "start": "pnpm run build && electron .",
    "preview": "vite preview",
    "pack": "pnpm run build && electron-builder --dir",
    "dist": "pnpm run build && electron-builder",
    "dist:win": "pnpm run build && electron-builder --win"
  },
  "author": "Clawdbot",
  "license": "MIT",
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "electron-log": "^5.2.4",
    "electron-store": "^8.2.0",
    "json5": "^2.2.3",
    "node-pty": "^1.1.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "wait-on": "^8.0.3"
  }
}
